server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/taxi_notification
    username: user_admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update # Flyway сам создаст таблицы, Hibernate просто проверяет
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    flyway:
      enabled: true
      baseline-on-migrate: true